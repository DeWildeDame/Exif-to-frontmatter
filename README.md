# Exif-to-frontmatter

Simple exif data parser that ouputs yaml front matter, 
there are probably more robus options out there, not the point of this library.

To run this tool:

```
pnpm i
pnpn build
node ./dist/cli.js ./photos/test.jpg
```

